# DEV_LOG

마지막 갱신 시각: 2026-05-11 23:45:26 KST

## 1. 프로젝트 개요

`excit`는 단순 생존게임보다 `정화 -> 개발 -> 거주 -> 운영` 루프를 중심으로 가는 메타버스형 지역 개발 게임이다.

핵심 방향:
- 오염된 지역을 정화해 새 생활권을 연다.
- 열린 공간을 플레이어가 개발하고 운영한다.
- 필지/거주/상점/전시 같은 권한이 점점 플레이어별로 분리된다.
- NFT는 장식보다 `공간 권한`, `거주권`, `운영권` 쪽으로 연결하는 방향을 우선한다.

---

## 2. 현재 구현된 기능

### 맵 / 공간
- `광산맵`, `폐광맵`, `개척지`가 존재한다.
- `개척지`에는 `P1 ~ P6` 필지가 있다.
- 개척지 서쪽에는 별도 `주거 구역`과 `Mansion ONE` 거주동이 있다.

### 인벤토리 / 장비 / 아이템
- 장비 / 소비 / 기타 인벤토리 탭 구조가 있다.
- 소비/기타 스택 최대치는 `200`.
- 권한 아이템, 퀘스트 핵심 아이템, NFT 아이템은 일반 아이템처럼 다루지 않는다.
- 개인 거주 공간 안에 `개인 창고`가 있다.

### 필지 / 건축
- `P1 ~ P6`는 각각 개발권 아이템으로 권한을 가진다.
- 필지 건축은 단계형 진척도로 진행된다.
- 완공된 필지에는 내부 운영 부스가 생성된다.

### 운영 / 상점 / 전시
- 모든 필지 공통 운영 구조:
  - 상점 슬롯 1개
  - 전시 슬롯 A
  - 전시 슬롯 B
- 판매자는 상점 슬롯에 상품을 등록할 수 있다.
- 방문자는 상점 부스 앞에서 `E`로 판매 보기 UI를 열고 구매할 수 있다.
- 판매 상품은 가판대 위에 실제 item visual로 표시된다.
- 결제 수단은 별도 잔액 값인 `개척 코인`이다.

### 거주
- `Mansion ONE 101호`, `Mansion ONE 102호`가 존재한다.
- 각 방은 포탈형 개인 거주 공간이다.
- 방 안에는 출구 문, 침대, 개인 창고가 있다.
- 침대에서 `Space : 취침` -> 취침/기상 UI -> 로그아웃 흐름이 있다.

### 개발자 테스트 환경
- 개발자 모드에서 `개발자1`, `개발자2`를 상단 UI로 전환할 수 있다.
- `개척지` 관련 월드 상태는 공용 공유 상태로 테스트된다.
- `테스트 초기화` 버튼이 있다.

개발자 역할:
- 개발자1:
  - 운영자 / 판매자 테스트용
  - `P1 ~ P6 개발권`
  - `Mansion ONE 101호`
  - `개척 코인 500원`
  - `소비/기타 스택형 주요 아이템 200개`
- 개발자2:
  - 방문자 / 구매자 테스트용
  - 필지 개발권 없음
  - `Mansion ONE 102호`
  - `개척 코인 500원`

---

## 3. 중요한 코드 구조

### 핵심 파일
- 메인 구현 파일: [src/main.js](/Users/yooseokjun/Desktop/voxel-town/src/main.js)
- 변경 기록: [CHANGELOG.md](/Users/yooseokjun/Desktop/voxel-town/CHANGELOG.md)
- 현재 문서: [DEV_LOG.md](/Users/yooseokjun/Desktop/voxel-town/DEV_LOG.md)

### 구조적으로 중요한 상태
- `frontierBuildState`
  - 개척지 `P1 ~ P6` 건축/운영 상태
  - 공용 월드 상태로 다뤄야 하는 핵심 데이터
- 개발자 세션 분리
  - 개발자1/개발자2는 개인 인벤토리/개인 창고/개인 거주 상태는 분리
  - 필지 건축/상점/운영 상태는 공유
- `개척 코인`
  - 인벤토리 아이템이 아니라 별도 잔액 값
- `Mansion ONE 101호`, `Mansion ONE 102호`
  - 권한 아이템 기반으로 입장하는 포탈형 개인 방

### 개념적으로 분리된 UI
- `B`
  - 필지 단위 건축 / 건축 관리
- `E`
  - 부스 단위 상호작용
  - 상점 보기 / 판매 관리 / 전시 보기 / 전시 관리
- `Space`
  - 침대 취침, 창고 열기 등 거주 공간 상호작용

### 앞으로 계속 재사용해야 하는 구조
- 새 충돌 오브젝트 생성 시 플레이어 끼임 방지 공통 보정 시스템
- 권한 아이템 / 퀘스트 핵심 아이템 / NFT 아이템의 예외 처리 규칙
- 개발자 테스트 초기화 후 역할별 프리셋 복원 구조

---

## 4. 지금까지 정한 개발 방향

### 큰 방향
- `excit`는 생존보다 `개척과 생활권 개발`이 중심이다.
- 공기 정화는 생존 페널티보다 `새로운 땅을 여는 과정`에 가깝다.
- 필지 개발권, 거주권, 운영권 같은 권한 구조를 점차 세분화한다.

### NFT 방향
- NFT는 단순 수집품보다 권한 구조에 연결한다.
- 예:
  - 필지 개발권
  - 거주권
  - 상점/전시 슬롯 사용권
- 현재는 실제 NFT가 아니라 권한 아이템으로 먼저 구현하고, 나중에 치환한다.

### 경제 방향
- `개척 코인`을 전용 화폐로 사용한다.
- 기존 재료를 억지 화폐로 쓰지 않는다.
- 상점은 단순 장식보다 `살아가고 개발하는 데 필요한 흐름`과 연결한다.

### 공간 방향
- 개척지는 `개발/운영` 구역
- 주거동은 `생활/거주` 구역
- 이후 콘텐츠도 공간 체감 중심으로 설계한다.

---

## 5. 코드 수정 규칙

이 프로젝트에서 Codex는 아래 규칙을 지켜야 한다.

### 가장 중요한 규칙
- **가장 최근에 합의한 Codex 절약형 템플릿 기준으로만 패치한다.**
- 이전 대화의 다른 주제를 다시 가져와서 패치하면 안 된다.
- 최신 템플릿이 있으면 그 템플릿 범위가 최우선이다.

### 읽기 범위 규칙
- 코드 전체를 함부로 읽지 않는다.
- 사용자가 지정한 범위, 또는 최신 템플릿에서 허용한 부분만 읽고 수정한다.
- 관련 함수/구조만 좁게 읽는 방식을 우선한다.

### 보고 방식
- 패치 후에는 항상 아래를 구분해서 말한다.
  - 문법 체크 결과
  - 런타임 위험 포인트
- `node --check src/main.js`만 통과했다고 끝이 아니다.
- 문법 체크와 실제 실행 흐름은 다르다.

### 파일 수정 규칙
- 수동 파일 수정은 `apply_patch` 사용
- 불필요한 리팩토링 금지
- 사용자가 허락하지 않은 다른 영역 수정 금지

### 세이브/데이터 규칙
- 플레이어 세이브가 초기화되거나 덮어써지지 않도록 매우 조심
- 공용 월드 상태와 개인 세이브 상태를 섞지 않도록 주의

---

## 6. 함부로 바꾸면 안 되는 부분

다음은 작은 수정처럼 보여도 영향이 큰 영역이라 함부로 바꾸면 안 된다.

### 1. 세이브 구조
- 개인 세이브
- 개발자1/개발자2 분리 세이브
- 공용 개척지 월드 상태 저장 구조

### 2. 권한 판정 구조
- 필지 개발권 아이템
- Mansion ONE 101/102호 권한
- 상점 슬롯 사용자 / 운영자 권한 분기

### 3. 개척지 필지 상태
- `frontierBuildState`
- 필지 완공도
- 상점/전시/판매 상태

### 4. 개인 거주 공간
- 101호 / 102호 입장 분기
- 침대 / 창고 / 출구 상호작용

### 5. 개발자 테스트 환경
- 개발자1 = 운영자/판매자
- 개발자2 = 방문자/구매자
- 테스트 초기화 버튼 동작

### 6. UI 입력 키 규칙
- `B` = 필지 건축/건축 관리
- `E` = 부스/상호작용 보기/관리
- `Space` = 침대/창고 같은 생활 상호작용

---

## 7. 윈도우에서 작업을 이어갈 때 Codex가 먼저 읽어야 할 내용

새 환경에서 작업을 이어갈 때는 아래 순서로 읽는 것이 좋다.

### 반드시 먼저 읽기
1. [DEV_LOG.md](/Users/yooseokjun/Desktop/voxel-town/DEV_LOG.md)
2. [CHANGELOG.md](/Users/yooseokjun/Desktop/voxel-town/CHANGELOG.md)
3. [src/main.js](/Users/yooseokjun/Desktop/voxel-town/src/main.js) 안에서 현재 작업하려는 범위의 관련 함수만

### Codex가 먼저 이해해야 하는 것
- 이 프로젝트는 `최신 템플릿 우선` 규칙이 있다
- 개발자1/개발자2는 역할이 다르다
- 개척지 월드 상태는 공용, 개인 인벤토리는 분리
- `B / E / Space` 상호작용 의미가 다르다
- 건축 UI는 유지하고, 운영 UI는 부스 상호작용으로 분리하는 방향이다

### 새 환경에서 작업 시작 전 체크
- 개발자모드 진입이 되는지
- 개발자1/개발자2 전환이 되는지
- 테스트 초기화가 정상 동작하는지
- Wallet에 `개척 코인`이 역할별로 정상 표시되는지
- 개척지 / Mansion ONE / 상점 부스 / 창고 / 침대 기본 기능이 살아 있는지

---

## 현재 기준으로 다음 단계 후보

다음 패치 후보는 대체로 아래 순서 중 하나로 이어질 가능성이 높다.

- 전시 슬롯 실제 관리/표시 기능 강화
- 상점 구매/판매 UX 보강
- 필지 운영 권한 구조 세분화
- 거주 기능 확장
- 공용 월드 상태와 테스트 환경 안정화

---

## 메모

- 대화 로그는 새 컴퓨터에서 그대로 이어지지 않을 수 있다.
- 그래서 이 문서를 기준으로 현재 맥락을 복원해야 한다.
- 이후 중요한 합의가 생기면 이 문서도 같이 업데이트하는 것이 좋다.
