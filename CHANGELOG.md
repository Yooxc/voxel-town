# CHANGELOG

## 2026-05-14

### main.js 8차 분리: 맵 상수/전환·연결통로·시작링 판정 헬퍼를 `src/systems/maps.js`로 이동
- `main.js` 안에 있던 광산/폐광/개척지/거주구역 좌표 상수, 현재 맵 판정, 연결통로 여부 판정, 공기용 유효 맵 판정, 시작링 충돌/관통 차단 같은 순수 맵 로직을 새 파일 `src/systems/maps.js`로 분리했다.
- `main.js`에는 여전히 Three.js 메쉬 생성, 실제 맵 배치, 개척지 운영과 거주 시스템처럼 부수효과가 큰 흐름만 남기고, 맵 책임이 분명한 상수/계산 함수는 maps 모듈을 import해서 사용하도록 정리했다.
- 이번 8차 분리도 기능 변경이 아니라 코드 이동/정리에 집중했고, 광산↔폐광↔개척지 전환, 연결통로 판정, 시작링 충돌 보정 같은 기존 동작을 그대로 유지하는 것을 목표로 했다.

### main.js 7차 분리: 공기 시스템 계산/정화율 헬퍼를 `src/systems/air.js`로 이동
- `main.js` 안에 있던 공기 게이지 상수, 오염 맵 설정, 맵 정화율 clamp/조회/초기화, 공기 감소/회복 판정, 오염 시각 강도와 저산소 오버레이 강도 계산 같은 순수 공기 시스템 로직을 새 파일 `src/systems/air.js`로 분리했다.
- `main.js`에는 여전히 HUD DOM 업데이트, 아이템 소비, 실제 플레이어 수치 반영, 맵 오브젝트와 연결된 부수효과 로직만 남기고, 공기 책임이 분명한 계산 함수는 air 모듈을 import해서 사용하도록 정리했다.
- 이번 7차 분리도 기능 변경이 아니라 코드 이동/정리에 집중했고, 오염 구역 공기 감소, 안전/정화 구역 회복, HUD 표시, 정화 완료 후 회복 흐름은 기존과 동일하게 유지하는 것을 목표로 했다.

### main.js 6차 분리: 채굴 메타데이터/계산 헬퍼를 `src/systems/mining.js`로 이동
- `main.js` 안에 있던 곡괭이 강화 단계 메타데이터, 바위 크기 정의, 채굴력 계산, 다음 강화 단계 계산, 바위 리스폰용 크기 조회, 나무 재활성 쿨다운/흔들림 계산 같은 채굴 시스템의 순수 로직을 새 파일 `src/systems/mining.js`로 분리했다.
- `main.js`에는 여전히 Space 입력 처리, Three.js 메쉬 생성, 실제 자원 지급, 채굴 연출과 월드 반영 같은 부수효과 로직만 남기고, 계산/정의 책임은 mining 모듈을 import해서 사용하도록 정리했다.
- 이번 6차 분리도 기능 변경이 아니라 코드 이동/정리에 집중했고, 바위 채굴, 곡괭이 레벨 제한, 나무 채집과 흔들림, 채굴 보상 흐름은 기존과 동일하게 유지하는 것을 목표로 했다.

### main.js 5차 분리: 인벤토리/장비창 UI 생성을 `src/ui/inventoryWindow.js`로 이동
- `main.js` 안에 있던 인벤토리 창, 장비창, 탭 버튼, 드래그 휴지통, 버리기 다이얼로그, 개인 창고 창과 수량 이동 다이얼로그의 DOM 생성/레이아웃 코드를 새 파일 `src/ui/inventoryWindow.js`로 분리했다.
- `main.js`에는 렌더링 시점의 상태 반영, 알림/입력과 연결되는 흐름만 남기고, 실제 창과 패널을 만드는 책임은 inventoryWindow 모듈이 맡도록 정리했다.
- 이번 5차 분리도 기능 변경이 아니라 코드 이동/정리에 집중했고, 인벤토리 열기/닫기, 탭 전환, 장비창 표시, 드래그 앤 드롭, 개인 창고 UI 흐름은 기존과 동일하게 유지하는 것을 목표로 했다.

### main.js 4차 분리: 인벤토리 상태/저위험 조작 헬퍼를 `src/systems/inventory.js`로 이동
- `main.js` 안에 있던 인벤토리/개인 창고 기본 상태 생성, 퀵슬롯 바인딩 순수 헬퍼, 슬롯 탐색/추가/소비/장착 참조 같은 저위험 인벤토리 조작 로직을 새 파일 `src/systems/inventory.js`로 분리했다.
- `main.js`에는 인벤토리 UI 렌더링, 알림 표시, 퀘스트 갱신, 세이브 동기화처럼 부수효과가 큰 흐름만 남기고, 실제 슬롯 데이터를 만지는 기초 연산은 inventory 모듈을 import해서 사용하도록 정리했다.
- 이번 4차 분리도 기능 변경이 아니라 코드 이동/정리에 집중했고, 아이템 줍기/소비, 장착/해제, 개인 창고 이동, 퀵슬롯 바인딩 같은 기존 동작을 그대로 유지하는 것을 목표로 했다.

### main.js 3차 분리: 지갑/세션 순수 헬퍼를 `src/auth/wallet.js`로 이동
- `main.js` 안에 있던 지갑 세션 기본 상태 생성, 주소 축약, 개발자 프로필 ID 정규화, 세션 직렬화/파싱, 세션 타입 판정, 체인 라벨 표시용 순수 헬퍼를 새 파일 `src/auth/wallet.js`로 분리했다.
- `main.js`에는 여전히 로그인 UI 갱신, 실제 세션 전환, 월드 초기화/세이브와 연결되는 부수효과 로직만 남기고, 순수한 계산/판정 로직은 wallet 모듈을 import해서 사용하도록 정리했다.
- 이번 3차 분리도 기능 변경이 아니라 코드 이동/정리에 집중했고, 개발자 모드/게스트/메타마스크 로그인 복원 흐름은 기존과 동일하게 유지하는 것을 목표로 했다.

### main.js 2차 분리: 플레이어 세이브/공용 월드 직렬화 헬퍼를 `src/save/playerSave.js`로 이동
- `main.js` 안에 있던 플레이어 세이브 기본 구조 생성, 세이브 직렬화, 세이브 입력 정규화, 공용 월드 상태 직렬화/정규화 관련 순수 로직을 새 파일 `src/save/playerSave.js`로 분리했다.
- `main.js`에는 로컬 저장소 접근, 서버 저장 호출, 실제 월드 상태 적용 같은 부수효과 로직만 남기고, 데이터 구조를 만드는 부분은 save 모듈을 import해서 쓰도록 정리했다.
- dev1/dev2 개인 세이브와 개척지 공용 월드 상태 공유 흐름은 그대로 유지하고, 이번 턴도 기능 변경이 아니라 코드 이동/정리에 집중했다.

### main.js 1차 분리: 아이템 정의/순수 헬퍼를 `src/systems/items.js`로 이동
- `main.js` 안에 있던 아이템 정의, 개발자 지급용 아이템 목록, 인벤토리 엔트리 정규화/표시 관련 순수 헬퍼를 새 파일 `src/systems/items.js`로 분리했다.
- `main.js`에서는 기존 함수명과 호출 흐름을 유지할 수 있도록 얇은 래퍼만 남기고, 실제 구현은 새 items 모듈을 import해서 사용하도록 정리했다.
- 이번 1차 분리는 기능 변경이 아니라 코드 이동/정리에만 집중했고, 개척지/상점/전시/거주/개발자 모드 등 기존 동작은 그대로 유지하는 것을 목표로 했다.

## 2026-05-12

### 개척지 전시 슬롯 실제 관리/표시 연결
- 개척지 전시 슬롯 `A/B`가 더 이상 단순 상태 문구만 가지지 않고, 실제 전시 대상 스냅샷(`entry`)을 함께 저장/복원하도록 확장했다.
- 운영자는 `E : 전시 관리`에서 인벤토리 안의 전시 가능한 대상들을 바로 골라 등록하거나 `전시 해제`로 비울 수 있게 했고, 방문자는 `E : 전시 보기`에서 현재 전시 중인 대상을 확인할 수 있게 정리했다.
- 전시 슬롯에 대상이 등록되면 부스 위 월드 비주얼도 함께 갱신되도록 연결해, UI의 전시 상태와 공간 안 실제 표시가 같은 내용을 보여주도록 보강했다.

## 2026-05-12

### 개척지 상점 UX 상태 표시/구매 피드백 보강
- 판매자 관리창에서 현재 판매 상태를 `상품 / 재고 / 개당 가격 / 상점 사용자`까지 한눈에 보이도록 보강했다.
- 구매자 보기창에서 입력한 수량 기준 총 가격과 `바로 구매 가능 / 재고 부족 / 개척 코인 부족 / 인벤토리 공간 부족 / 자기 상품 구매 불가` 같은 상태를 즉시 피드백하도록 정리했다.
- 구매 버튼 비활성화와 안내 문구를 실제 구매 가능 여부와 같은 기준으로 묶어, 상점 UI와 실제 구매 결과가 덜 어긋나게 보강했다.

### 개발자 공유 월드에 시작 지역 NFT 전시보드와 거주구역 게시판 상태 포함
- 시작 지역 NFT 전시보드 선택값 `displayBoard`를 개발자 공용 월드 상태로 옮겨 개발자1/개발자2 전환 시 같은 전시 작품이 보이도록 정리했다.
- 개발자 세션에서는 개인 저장본이 `displayBoard`를 다시 덮지 않도록 보정하고, NFT 전시 선택 변경 시 공용 월드 저장도 즉시 갱신되게 연결했다.
- 거주구역 `게시판 A/B/C`의 상태를 공용 월드 구조에 추가하고, 각 게시판이 독립된 제목/문구 배열 상태를 기준으로 화면 텍스처를 다시 그리도록 기반 구조를 만들었다.

### Mansion ONE 서쪽 땅 `거주구역` 지역명/안전 구역 처리
- `Mansion ONE`이 있는 서쪽 땅의 지역명을 `거주구역`으로 정의하고, 해당 바닥과 방 내부가 `거주구역` 맵 판정을 받도록 연결했다.
- 플레이어가 거주동 생활권으로 이동하면 상단 맵 진입 알림에 `거주구역`이 표시되도록 현재 맵 판정 로직에 주거 구역 우선 분기를 추가했다.
- `거주구역`은 오염 맵 목록에 넣지 않아 공기 정화 없이도 항상 안전 구역처럼 동작하게 정리했다.

### 개발자 공유 월드에 폐광 잠금 해제와 공기 정화율 포함
- 개발자1/개발자2가 공유하는 `DEV_SHARED_WORLD_KEY` 월드 상태에 `폐광맵 잠금 해제 상태`와 `맵별 공기 정화율`을 추가했다.
- 개발자 프로필 전환 시 개인 세이브를 불러오더라도, 개발자 세션에서는 위 두 값이 개인 저장값으로 다시 덮이지 않고 공유 월드 기준으로 유지되도록 보정했다.
- `폐광 열쇠` 사용으로 통로를 해제할 때도 즉시 공유 월드 저장이 갱신되도록 저장 동기화 호출을 추가했다.

### 개발자모드 테스트 초기화 공기 오염도 리셋 수정
- 개발자모드에서 `테스트 초기화`를 실행해도 `개척지` 공기 정화율이 이전 값으로 남던 문제를 수정했다.
- 기존 초기화 경로가 `폐광맵` 정화율만 0으로 되돌리던 것을, `MAP_POLLUTION_CONFIG`에 등록된 모든 오염 맵 정화율을 함께 초기화하도록 보강했다.
- 개발자 프리셋 적용과 일반 새 시작 상태 모두 같은 공기 정화율 초기화 헬퍼를 사용하도록 정리했다.

## 2026-05-11

### 개발자 계정 전환 복구
- `개발자1 / 개발자2` 전환 시 새 프로필을 선택해도 초기화 함수가 이전 활성 프로필을 다시 읽어와 되돌리는 문제를 수정
- 전환 버튼 클릭 시 선택한 개발자 프로필을 먼저 활성 키에 기록하고, 그 프로필을 우선 적용하도록 흐름을 바로잡아 상단 버튼 강조와 개인 세이브 전환이 실제 선택과 일치하게 정리

### 개발자1 / 개발자2 공용 월드 테스트 환경 1차
- 개발자 모드 진입 시 상단에 `개발자1`, `개발자2` 전환 UI를 추가하고, 각 선택이 `dev_user_1`, `dev_user_2`로 분리된 개인 테스트 계정처럼 동작하도록 확장
- 인벤토리, 장비, 튜토리얼, 개인 창고, 개인 위치 등은 개발자 계정별 로컬 저장으로 분리하고, 개척지 `P1~P6` 필지 건축/운영/상점 상태는 `공용 월드 상태`로 따로 저장하도록 개발자 모드 전용 저장 흐름을 추가
- 개발자1이 만든 필지 건축 상태나 판매 등록 상태를 개발자2가 같은 세계에서 이어서 확인할 수 있도록, 개발자 계정 전환 시 `개인 세이브 + 공용 월드 상태`를 함께 저장/복원하는 테스트 환경을 보강

### 오브젝트 생성 시 플레이어 끼임 방지 1차
- 새 충돌 오브젝트 생성 직후 플레이어 박스와 겹침을 검사하고, 겹치면 안전 위치로 자동 보정하는 `ensurePlayerNotInsideGeneratedColliders()` 공통 헬퍼를 추가
- 개척지 `P1~P6` 건축 단계 상승과 운영 부스 생성 흐름에 이 보정 로직을 연결해, 건물이나 부스가 플레이어 위치에 생성될 때 필지 입구 쪽 안전 위치로 자동 이동되도록 보강
- 개인 거주 공간의 문/침대/창고 가구 배치에도 같은 안전 보정 흐름을 연결해, 앞으로 생성형 가구/장치에도 재사용할 수 있는 공통 규칙 형태로 정리

### 개척지 상점 슬롯 사용자 지정 1차
- 개척지 `P1~P6` 공통 운영 데이터의 상점 슬롯에 `userWallet`을 추가하고, 운영자가 상점 사용자를 `내가 직접 사용 / 지갑 주소로 지정 / 사용자 해제` 방식으로 관리할 수 있게 확장
- 상점 슬롯 사용자는 필지 개발권이 없어도 판매 등록 UI를 열 수 있게 하고, 운영자라도 상점 사용자로 지정되지 않으면 판매 등록을 할 수 없도록 권한 흐름을 분리
- 완공 필지의 운영 관리 창에 상점 슬롯 사용자 관리 영역과 현재 지정 사용자 표시를 추가하고, 힌트 문구도 `상점 사용자 지정 필요` / `B : 운영 관리` / `Pn 개발권 또는 상점 권한 필요` 흐름으로 보강

### 개척지 필지 판매 등록 UI 1차
- 개척지 `P1~P6` 공통 운영 상태의 상점 슬롯을 `아이템 / 수량 / 가격` 기준으로 확장하고, 완공된 필지에서 `판매 등록` 창을 열어 실제 판매 물품을 등록/회수할 수 있도록 보강
- 판매 등록 창에는 인벤토리의 판매 가능한 재료/소비 아이템 목록, 판매 수량, 가격 입력을 두고, 장비 / 권한 / 퀘스트 핵심 / NFT 아이템은 등록 대상에서 제외되도록 정리
- 상점 슬롯에 상품이 등록되면 완공 건축물 내부 `상점` 부스 위에 기존 아이템 비주얼 소스를 재사용한 상품 표시와 수량 표식이 함께 보이도록 연결
- 판매 등록/해제 내용은 기존 필지 운영 데이터와 함께 저장/복원되며, `Esc`로 판매 등록 창을 우선 닫을 수 있게 정리

### 개척지 필지 내부 운영 부스 1차
- 개척지 `P1~P6` 필지가 건축 `100%` 완료되면, 각 건축물 내부에 공통 구조의 운영 부스가 실제로 보이도록 `상점 1개`, `전시 A`, `전시 B` 부스를 추가
- 부스마다 `상점`, `전시 A`, `전시 B` 라벨과 현재 운영 상태 텍스트를 함께 표시해, 기존 운영 UI의 `비어 있음` 상태가 공간 안에서도 그대로 읽히게 정리
- 부스 자체에 기본 충돌을 넣고 중앙 이동 동선은 비워둬, 실제 상점/NFT 전시 기능 연결 전에도 완공된 필지가 운영 공간처럼 느껴지도록 보강

## 2026-05-09

### 개척지 P1~P6 공통 운영 UI
- 개척지 `P1~P6` 필지 건축 데이터에 공통 운영 상태를 추가하고, 각 필지가 완공되면 `상점 슬롯 1개`, `전시 슬롯 2개`의 상태를 함께 저장/복원할 수 있도록 확장
- 기존 건축 창을 완공 후 `운영 관리` 화면으로 전환해, 각 필지의 현재 운영 슬롯 상태를 `비어 있음` 기준으로 확인할 수 있도록 정리
- 완공 필지 위 힌트 문구를 `B : 간판 수정`에서 `B : 운영 관리`로 바꾸고, 실제 상점/전시 기능 연결 전에도 모든 필지에서 동일한 운영 구조를 먼저 체감할 수 있게 조정

### Mansion ONE 개인 창고 개수 선택
- 개인 거주 공간 창고 UI에 `옮길 개수` 확인창을 추가해, 인벤토리와 개인 창고 사이에서 스택 아이템을 드래그할 때 전체 개수 대신 원하는 수량만 입력해서 이동할 수 있도록 확장
- 창고 -> 인벤토리, 인벤토리 -> 창고 양방향 모두 부분 이동을 지원하고, 입력값이 잘못되거나 공간이 부족할 때는 이동을 취소하고 안내 문구를 표시하도록 보강
- `Esc`로 개수 선택창만 먼저 닫히도록 우선순위를 추가해, 창고 UI를 닫지 않고도 수량 입력만 취소할 수 있게 정리

### 개척지 서쪽 주거 구역 연결 보강
- 개척지 서쪽 연결 통로 바닥과 주거 구역 공용 광장 바닥 사이에 연결 슬랩을 추가해, 두 구역이 시각적으로 끊기지 않고 실제 보행 동선도 자연스럽게 이어지도록 수정
- 주거 구역 바닥 범위를 동서남북으로 더 넓혀 거주동 주변에 여유 공간이 생기도록 확장하고, `게시판` / `공용 표지`를 실제 팻말로 보이게 배치
- 거주동 정면 문 장식은 상부 프레임을 제거한 기둥형 파사드로 바꾸고, 건물 간판은 우측으로 옮겨 이름을 `Mansion ONE`으로 갱신
- 공용 광장 위에 겹쳐 있던 바닥 메쉬를 정리해 화면이 우글거리는 z-fighting 현상을 제거하고, 광산맵 전시 보드와 같은 형태의 비기능 게시판 3개를 배치
- 주거 구역 외곽과 연결 통로 양옆에 캐릭터 높이의 담을 추가해, 경계 바깥으로 떨어질 것 같은 느낌이 들도록 생활 구역 경계를 강화
- 연결 통로 양옆 담 좌표가 뒤바뀌어 통로를 가로막던 문제를 수정하고, 이제 담이 통로 좌우 가장자리를 따라 서도록 보정
- 위 보정 후에도 축 방향이 반대로 잡혀 통로를 다시 막던 문제를 한 번 더 수정하고, 이제 연결 통로 담은 `x축으로 길고 z축으로 얇은` 형태로 양옆 경계에만 배치
- 연결 통로와 주거 구역이 만나는 동선 기준으로 담 배치를 다시 정리해, 통로 중앙은 완전히 열고 외곽 경계만 막히도록 보정

### Mansion ONE 101호 취침/로그아웃
- 개인 거주 공간 침대에 보이지 않는 상호작용 마커를 추가해, 침대 근처에서 `Space : 취침` 힌트가 뜨고 Space로 취침 UI를 열 수 있도록 구성
- 취침 중에는 캐릭터 이동을 잠그고 침대에 누운 듯한 자세로 보이게 하며, 화면을 흐리게 처리한 `로그아웃하시겠습니까?` 대화상자를 띄우도록 연결
- `취침`을 누르면 실제 로그아웃 흐름으로 이어지고, `기상` 또는 배경 클릭 시 침대 옆 안전한 위치로 되돌아오도록 정리
- 취침 UI 상태 변수 선언이 실제 UI 생성보다 아래에 있어 개발자 모드 입장 시 런타임 오류가 나던 문제를 수정하고, 선언을 상단으로 이동해 안정화

### 개척지 서쪽 주거 구역
- 개척지 서쪽 벽에 주거 구역 출입구를 새로 내고, 그 바깥으로 연결 바닥과 작은 공용 광장을 추가해 개발 구역과 생활 구역이 분리된 구조로 확장
- 공용 광장 너머에 5층짜리 거주동 외형 1채를 배치하고, 전면 창·출입구 차양·상단 간판을 더해 실제 정착 구역처럼 보이도록 구성
- 주거동과 주요 구조물에는 기본 충돌을 넣어 플레이어가 건물을 뚫고 지나가지 않도록 보강하고, 서쪽 빈 공간은 향후 주거 확장용으로 남겨둠

### 개척지 필지 건축물 방향
- 개척지 `P1`, `P3`, `P5` 건축물은 좌측 필지 특성에 맞게 중앙 세로 통로를 향하도록 정면/간판/뒷벽 방향을 반전해, 좌우 필지 건물들이 모두 통로를 바라보는 거리형 배치로 보이게 조정

### 개척지 필지 개발권 아이템
- 개척지 `P1~P6` 각각에 대응하는 임시 권한 아이템 `개척지 Pn 개발권` 6종을 추가하고, 향후 NFT 권한으로 교체하기 쉽도록 필지 라벨 기준 권한 판정 흐름으로 정리
- 권한 아이템은 기타 아이템으로 취급하되 버리기 금지로 보호하고, 휴지통 드롭 시도 시 `권한 아이템은 버릴 수 없습니다` 안내가 뜨도록 처리
- 개척지 필지 건축/간판 수정 권한을 `개척지 Pn 개발권` 보유 여부 기준으로 바꾸고, 권한이 없는 플레이어는 `Pn 개발권 필요` 힌트만 보이도록 수정
- 개척지 건축 상태 저장값을 `P1~P6` 전체 필지 기준으로 일반화하고, 기존 `P6`만 있던 저장 데이터도 그대로 읽을 수 있게 호환 처리
- 개발자 모드에서는 테스트를 위해 `개척지 P1~P6 개발권` 아이템 6개를 모두 자동 지급하도록 연결
- 권한 일반화 과정에서 빠졌던 `rebuildFrontierParcelConstructionVisual()` 함수 본문을 복구해, 개발자 모드 입장 시 하얀 화면처럼 보이던 런타임 오류를 수정

## 2026-05-08

### 개척지 P6 건축 슬롯
- 개척지 `P6` 필지 안에서만 `B` 키로 열 수 있는 첫 건축 UI를 추가하고, 목재/석재를 한 번에 차감해 `0% -> 25% -> 50% -> 100%`로 진행하는 단계형 건축 흐름을 구현
- P6 건축물은 단계에 따라 기초 마커, 기둥, 벽, 천장이 순차적으로 생기도록 만들고, 세로 메인 통로 방향 정면은 열린 구조로 유지
- 건축 진행 상태와 간판 텍스트를 플레이어 저장 데이터에 포함해 새로고침/재접속 후에도 P6 건축 진척도와 간판 문구가 복원되도록 연결
- 건축이 100% 완료되면 한글/영문 입력이 가능한 간판 텍스트 편집 UI를 열 수 있게 하고, 다른 UI와 겹치지 않도록 `Esc` 및 기존 창 열기 흐름과 함께 정리
- 건축 UI 추가 직후 하얀 화면이 뜨던 초기화 순서 버그를 수정하고, `frontierBuildWin` 생성 전 `updateInventoryUI()`가 참조하지 않도록 선언 순서를 안정화
- `updateInventoryUI()`가 초기 실행 시 `frontierBuildWin.style`을 바로 읽으며 다시 하얀 화면이 뜨던 문제를 수정하고, 건축 UI가 아직 생성되지 않은 경우에는 렌더를 건너뛰도록 안전 가드 추가

## 2026-05-06

### 입력 / UI 편의성
- 소비 아이템 단축키 지정 시 `1~5` 숫자키가 지정과 동시에 바로 사용까지 되는 버그를 수정하고, 지정 직후 입력은 사용 로직으로 이어지지 않도록 차단
- 주요 입력 판정을 `event.key` 중심에서 `event.code` 기반으로 보강해, 한/영 전환 상태와 무관하게 `E / I / Q / 1~5 / Shift` 등이 안정적으로 동작하도록 정리
- `Esc` 키로 NFT 전시 선택창, 재련대 UI, 강화창, 인벤토리, 퀘스트 창을 순서대로 닫을 수 있도록 공통 닫기 흐름 추가
- 인벤토리 창이 열려 있을 때 `Tab` 키로 `장비 -> 소비 -> 기타` 탭을 순환 이동할 수 있도록 편의성 추가

### NFT 전시 보드
- 작품 하단 캡션을 2줄 구조로 재배치해 제목과 소유자 표기가 서로 침범하지 않도록 조정
- 기존 `소유자 확인 완료` 문구를 제거하고, 실제 로그인 닉네임 기준 `소유자: 닉네임` 형태로 표기되도록 변경

### 개발자 프리셋 / 장착 참조
- 개발자 모드 시작 시 곡괭이와 안전모를 문자열 종류값이 아니라 실제 인벤토리 슬롯의 `instanceId` 기준으로 장착하도록 보정
- 이제 개발자 모드에서 같은 이름의 장비를 새로 주워도, 기존 장착품과 새 획득품이 동시에 장착된 것처럼 표시되지 않도록 수정

### 인벤토리
- 인벤토리 창 하단에 별도 줄 형태의 휴지통 드롭존을 추가해서, 기존 슬롯 영역을 침범하지 않고 아이템을 드래그해 버릴 수 있도록 구현
- 드롭 직후 `아이템 버리기` 팝업을 띄워 현재 보유 개수를 보여주고, 사용자가 버릴 숫자를 직접 입력한 뒤 확인하면 해당 수량만 버려지도록 추가
- 장착 중 아이템, NFT 아이템, 핵심 퀘스트 아이템은 버리기 대상에서 제외하고 안내 문구를 보여주도록 안전장치 추가
- 장비 아이템, NFT 아이템, 퀘스트 아이템도 드래그 자체는 가능하게 바꾸고, 실제 휴지통 드롭 시점에만 `버릴 수 없음` 안내가 뜨도록 UX를 정리
- 퀘스트와 연관된 아이템은 인벤토리 툴팁에 `퀘스트 아이템` 문구가 함께 보이도록 추가

## 2026-05-05

### UI / 프로필
- 우측 상단 상시 Wallet HUD를 숨기고, Wallet 정보를 `Equipment` 창 하단 프로필 칸으로 이동
- 장비창 하단에서 `지갑 주소 / 체인 / 닉네임 / 로그아웃`을 함께 확인할 수 있도록 정리
- 기존 로그아웃 동작은 유지하면서, 장비창 하단 프로필 칸의 로그아웃 버튼도 같은 흐름을 재사용하도록 통합
- `Equipment` 창 높이와 하단 여백을 늘려, Wallet 프로필 카드가 신발 장비칸과 장착 미리보기 하단을 가리지 않도록 레이아웃 보정
- Wallet 체인 표기를 `Ethereum Mainnet (0x1)`처럼 네트워크 이름과 체인 ID를 함께 보여주도록 개선
- Wallet 주소 옆에 복사 버튼을 추가해 긴 지갑 주소를 바로 클립보드로 복사할 수 있도록 보강

### 맵 / 게이트 충돌
- 폐광 입구 울타리 해제 후에도 캐릭터가 통과하지 못하던 문제를 수정
- 충돌체 제거 시 바위만 인덱스를 보정하던 구조를 바꿔, 모든 콜라이더 오브젝트의 `colliderIndex`를 재정렬하도록 개선
- 이제 울타리 메쉬가 사라질 때 실제 이동 판정도 같은 타이밍에 정확히 해제되도록 정리
- 폐광 게이트가 `lockColliderIndex` 숫자를 별도로 들고 있어 다른 충돌체 제거 후 값이 낡아질 수 있던 문제를 수정하고, 이제는 울타리 오브젝트가 가진 실제 `userData.colliderIndex`를 우선 참조하도록 보강

### 저장 / 불러오기 보호
- 지갑 로그인 직후 `기본 초기 상태`가 먼저 적용되더라도, 서버 세이브 기준선을 확인하기 전에는 자동 저장이 절대 실행되지 않도록 세이브 보호 상태를 추가
- 서버 세이브가 정상적으로 `불러오기 완료`되거나, 해당 지갑에 저장 데이터가 아예 없다는 것이 확인된 뒤에만 저장 동기화가 풀리도록 정리
- 세이브 불러오기 실패 시 `저장 보호 모드`로 남아 기본 상태가 서버 저장을 덮어쓰지 못하게 차단
- 서버 쪽 `playerSaveBackups` 백업 저장소를 추가해, 기존 저장 데이터를 새 저장으로 교체하기 전에 같은 지갑의 이전 저장 스냅샷을 최대 10개까지 보관하도록 확장
- 서버 저장 요청이 기존 진행도보다 훨씬 비어 있는 `기본 초기 상태`로 보일 경우, 해당 저장을 409로 거부해 진행도 초기화 덮어쓰기를 한 번 더 차단

## 2026-05-04

### 인벤토리 / 장착 UI
- 일반 장비 슬롯에도 `instanceId`를 부여해 같은 종류 장비가 여러 개 있을 때 실제로 장착한 그 슬롯 1개만 노란 테두리로 강조되도록 수정
- 일반 장비 장착 참조도 `itemId`뿐 아니라 `instanceId`를 함께 저장해, 곡괭이/안전모 복수 보유 시 장착 표시가 모든 동일 아이템에 번지던 문제 해결
- 장비 슬롯 더블클릭이 `itemId` 문자열만 넘기던 문제를 수정해, 실제 슬롯 엔트리를 기준으로 장착/강조가 동작하도록 보정
- 우측 상단 `마지막 패치` 시각을 현재 수정 시각으로 다시 동기화하고, 이후 의미 있는 패치마다 함께 갱신하는 기준을 재확인

### 공기 오염 / 정화 프로토타입
- `폐광맵`을 첫 오염 맵으로 설정하고, 공기 게이지 HUD와 맵 정화율 표시를 추가
- `신선한 공기 캔` 소비 아이템을 추가하고, 시작 작업대와 폐광 입구 근처에서 획득할 수 있도록 배치
- 소비 탭 아이템의 빠른 사용 방식을 `R` 고정 등록에서 `1~5` 지정형으로 확장하고, 슬롯의 빈 원형 버튼을 클릭한 뒤 원하는 숫자키를 눌러 단축키를 배정할 수 있도록 변경
- `신선한 공기 캔`은 소비 탭에서 더블클릭으로 바로 사용하거나, 지정한 `1~5` 단축키로 빠르게 사용할 수 있도록 유지
- 폐광 안에서는 정화율에 따라 공기 소모 속도가 달라지고, 정화 100% 달성 시 공기 소모가 멈추도록 구현
- 폐광 내부에 `공기 정화 장치`를 추가하고, `돌가루`를 소모해 정화율을 올릴 수 있는 1차 상호작용 루프를 구현
- 폐광 전용 오염 시각 연출 1차로, 맵 내부에 떠다니는 오염 입자 필드를 추가하고 정화율이 오를수록 입자 밀도가 줄어들도록 연결
- 산소가 부족할수록 화면 가장자리에 약한 탁한 비네트가 더해져, 플레이어가 오염 상태와 저산소 위험을 직접적으로 체감할 수 있도록 보강
- 폐광 오염 입자가 너무 약해 보이지 않도록, 입자 수를 늘리고 밝은 스프라이트 텍스처/가산 블렌딩을 적용해 공기층 오염이 눈에 띄게 보이도록 강화
- 오염 입자의 높이 분포를 플레이어 시야 높이 근처로 더 집중시키고, 입자 크기와 불투명도를 크게 올려 눈에 띄는 큰 먼지층처럼 보이도록 재조정
- 광산맵 시작 작업대 위에 `재련소`를 추가하고, `돌가루 3개 -> 정화 가루 1개`로 가공할 수 있는 첫 재련 루프를 구현
- 재련소를 즉시 제작 방식에서 레시피 선택형 UI로 확장하고, 현재는 `돌가루 3개 -> 정화 가루 1개` 한 가지 레시피만 표시되도록 1차 재련 인터페이스를 연결
- 재련소 UI는 `E` 키로 열고 닫을 수 있으며, 플레이어가 멀어지면 자동으로 닫히도록 동선 흐름을 정리
- 재련소 UI의 닫기 버튼을 우측 상단으로 옮기고, 가운데 화살표 영역 자체를 `재련하기` 실행 버튼으로 바꿔 입력/출력 흐름을 더 직관적으로 정리
- 재련 결과물의 현재 보유량을 UI 안에서 함께 보여주고, 재련 성공 시에는 창 내부 안내 패널과 버튼 문구가 잠깐 성공 상태로 바뀌어 결과를 바로 확인할 수 있도록 개선
- 재련대 외형을 작은 바구니형 소품에서 `투입구-가공 본체-정제 챔버-배출 트레이-게이지/레버`가 보이는 독립형 소형 공정 설비로 교체
- 재련대를 작업대 위에서 내려 강화 모루 남쪽 바닥으로 옮기고, 좀 더 큰 크기의 독립 설비처럼 보이도록 배치 조정
- 재련대 배치 순서를 정리해 강화 모루 좌표를 참조하기 전에 발생하던 실행 오류를 수정
- 재련대 근접 하이라이트를 본체 전체 발광에서 얇은 테두리 프레임 강조로 변경하고, 캐릭터가 설비를 통과하지 않도록 전용 충돌체를 추가
- 재련 성공 안내가 약 1초 뒤 원래 설명 문구로 자동 복귀하도록 창 내부 피드백 타이밍을 보강
- 재련대 하이라이트를 감지용 직육면체가 아니라 실제 재련대 주요 파츠 외곽선만 약하게 보이도록 다시 정리
- 재련대 근접 힌트 문구를 레시피 설명형에서 `E : 재련대 사용`으로 단순화
- 재련대 외형 하이라이트 연출은 사용자 선호에 맞지 않아 완전히 제거하고, 상호작용은 힌트 문구만으로 안내하도록 정리
- 강화 UI의 닫기 버튼을 재련대 UI와 같은 우측 상단 위치로 옮기고, 하단 닫기 버튼은 제거
- 폐광의 정화 시설을 `공기 정화탑` 흐름으로 정리하고, 이제 정화율 상승에는 `정화 가루`를 소모하도록 변경
- 공기 게이지와 폐광 정화율을 플레이어 세이브 데이터에 저장하고, 재로그인 시 복원되도록 확장
- 정화율 100%를 달성한 맵이나 기본적으로 호흡 가능한 맵에 들어가면 공기 게이지가 초당 소량씩 자동 회복되도록 보강
- 공기/정화 체감 테스트를 위해 폐광 진입 시 적용되던 주변 어둠 연출을 상수 토글로 잠시 비활성화
- 공기/정화 체감 테스트를 위해 폐광 전용 안개/시야 제한도 별도 토글로 잠시 비활성화
- 공기 게이지 HUD를 마우스로 드래그해 위치를 옮길 수 있도록 하고, 옮긴 위치를 로컬에 저장해 새로고침 후에도 유지되도록 개선

## 2026-05-03

### 개발 환경 로그인 UX
- `localhost` / `127.0.0.1` 개발 환경에서는 페이지가 다시 로드될 때 저장된 인증 세션을 자동으로 비워, 패치 후 항상 로그인 창부터 다시 시작되도록 조정

### NFT 연동 / 전시 보드
- 광산맵 강화모루 왼쪽에 캐릭터 높이의 `지갑 NFT 전시 보드` 추가
- 로그인한 메타마스크 지갑 기준으로 Ethereum 메인넷 `0x3aAA87FBe562AC659F9e657a8EFe00b411a56273 / tokenId 1` 보유 여부를 확인하는 1차 흐름 추가
- 보유 중이면 `ownerOf` / `tokenURI` / 메타데이터 이미지 URL을 읽어 보드에 실제 작품 이미지를 전시하도록 구현
- 미보유, 미로그인, 체인 불일치, 메타데이터 오류 상황에서는 보드에 안내 문구가 표시되도록 처리
- 어두운 NFT 작품이 전시 보드에서 너무 검게 뭉개져 보이지 않도록 밝기/대비 보정과 밝은 매트 배경을 추가
- 전시 보드 외형을 검은 프레임의 화이트보드 스타일로 변경하고, NFT 이미지가 흰 보드판 위에 부착된 느낌으로 수정
- 전시 보드 크기를 가로 3배, 높이 2배 수준으로 확대하고, NFT 이미지가 가능한 한 보드 면적을 꽉 채우도록 중앙 크롭 방식으로 조정
- 확대된 전시 보드가 플레이어 동선을 막지 않도록 강화모루에서 더 떨어진 위치로 옮기고 충돌 범위를 줄여 끼임 현상 완화
- 전시 작품이 잘리지 않도록 비율 유지 방식으로 되돌리고, 작품을 가리던 가로 프레임을 아래로 내리고, 보드 전체 높이를 약간 올려 시야를 정리
- 전시 작품을 가리던 중간 가로 프레임을 제거해 보드 중앙 시야를 완전히 열어둠
- 작품 위를 가로지르던 상단 연결 바도 제거해 전시 작품 상단 시야를 완전히 정리
- 보드 텍스처 캔버스 비율을 실제 전시 보드 화면 비율에 맞춰 조정해 NFT 작품이 위아래로 눌려 보이던 현상 완화
- 전시 보드의 양옆 지지대와 하단 프레임을 전시 면보다 뒤로 물려, 작품 위를 가로지르는 검은 선처럼 보이던 시각적 겹침을 줄임
- 전시 보드 앞에서 `E`로 상호작용하면 지갑이 보유한 전시 가능 NFT 목록을 팝업으로 열고, 원하는 NFT를 골라 보드 작품을 교체할 수 있는 1차 선택 기능 추가
- 선택한 전시 NFT는 플레이어 세이브(`displayBoard`)에 저장되어 같은 지갑으로 다시 로그인하면 보드 전시 상태가 복원되도록 확장
- NFT 전시 선택 팝업에 `NFT 전시 해제` 칸을 추가해, 작품 전시를 비우고 그 상태까지 세이브에 저장할 수 있도록 확장
- NFT 전시 선택 팝업에서 이미 전시 중인 작품을 다시 눌렀을 때 불필요한 저장이 일어나지 않도록 중복 선택 저장을 방지
- 지갑 NFT 목록 조회 결과를 짧게 캐시해, 같은 세션에서 전시 선택 팝업을 다시 열 때 체감 속도를 개선
- NFT 전시 선택 팝업에 `불러오는 중 / NFT 없음 / 네트워크 실패` 상태를 카드 영역에서도 더 분명하게 보여주도록 보강
- NFT 전시 선택 팝업을 열 때 인벤토리, 퀘스트, 강화창을 자동으로 닫아 입력 및 UI 충돌 가능성을 줄임
- NFT 전시 선택 팝업의 `NFT 없음` / `네트워크 실패` 상태 카드에 `다시 시도` 버튼을 추가해 바로 목록 재조회가 가능하도록 보강
- `displayBoard` 저장값에 대해 `contractAddress`, `chainId`, `tokenId`, `image` 형식 검증을 보강해 잘못된 NFT 전시 데이터가 세이브에 남는 상황을 줄임
- 전시 중이던 NFT를 더 이상 지갑이 보유하지 않을 때 자동 해제하지 않고, 보드와 선택창에서 `보유 불일치` 안내를 띄운 뒤 다시 선택하거나 해제하도록 정책을 정리

## 2026-05-01

### 저장 / 불러오기
- 정식 DB 전 단계로 `server/data/app.json` 기반 플레이 세이브/로드 1차 구조 추가
- 지갑 세션 기준으로 인벤토리, 장착 상태, 곡괭이 강화 레벨, 튜토리얼 진행, 폐광 해금, 현재 맵/위치를 서버 파일에 저장
- 메타마스크 로그인 후 기존 세이브가 있으면 자동으로 불러오고, 플레이 중에는 주기적으로 서버 파일에 자동 저장
- 로그인 직후 초기 상태가 기존 세이브를 덮어쓰지 않도록 세이브 동기화 일시정지 처리 추가
- 인벤토리 변화와 퀘스트 진행 변화가 생기면 4초 주기를 기다리지 않고 즉시 저장 시도하도록 보강
- 좌측 하단에 저장 상태 배지를 추가해 `저장 중...`, `저장됨`, `저장 실패` 상태를 조용하게 표시
- 브라우저 새로고침/탭 종료 직전에 `keepalive` 요청으로 마지막 세이브를 한 번 더 전송하도록 추가
- `updatedAt` / `lastKnownUpdatedAt` 비교를 추가해 오래된 탭의 저장 요청은 거부하고, 더 최신 저장이 있으면 최신 저장으로 다시 동기화되도록 보강

### 채굴 애니메이션
- 채굴 동작을 팔 위주 모션에서 전신 모션으로 확장
- 준비 자세에서 상체가 뒤로 실리고, 타격 시 몸통 회전·상체 전진·무릎 반응이 함께 들어가도록 조정
- 타격 후 반동에서 몸이 다시 회복되는 흐름을 추가해 정적인 느낌 완화

### 로그인 / 인증
- 인증 서버 CORS 허용 Origin을 `localhost`와 `127.0.0.1` 모두 받도록 확장해 로컬 접속 차이로 메타마스크 로그인이 막히는 문제 완화
- 인증 서버 바인딩 주소와 프론트 API 주소를 `127.0.0.1` 기준으로 통일해 로컬 루프백 해석 차이 문제 완화
- 로컬 개발 환경에서 실제 접속 기준인 `localhost`에 맞춰 프론트 인증 API 주소와 백엔드 기본 바인딩 주소를 다시 `localhost` 기준으로 정리

## 2026-04-30

### 로그인 / 인증
- 메타마스크 로그인 오버레이 UI 추가
- 지갑 연결 → 서명 → 로컬 세션 저장 흐름 1차 구현
- 로그인 후 지갑 주소를 표시하는 지갑 상태 HUD 추가
- 개발자 모드에서 로그인 우회용 `개발자 모드로 바로 입장` 버튼 추가
- 로그인 전에는 이동, 상호작용, 퀘스트/인벤토리 입력이 잠기도록 처리
- 이후 백엔드 nonce 검증으로 확장할 수 있도록 인증 상태 구조 분리
- 지갑 주소별 닉네임 생성/저장 오버레이 추가
- 닉네임이 설정되어야 실제 게임 조작이 가능하도록 변경
- `server/` 백엔드 구조 추가
- nonce 발급 / 서명 검증 / 세션 토큰 / 닉네임 저장용 인증 API 뼈대 추가
- 프론트 메타마스크 로그인 흐름을 `/auth/nonce`, `/auth/verify`에 연결
- 로그인 세션 복원을 `/auth/me` 기준으로 검증하도록 변경
- 닉네임 저장을 서버 `PATCH /auth/nickname` API 기반으로 전환
- 닉네임 저장 시 `Authorization` 헤더 추가로 `Content-Type`이 덮여 저장이 막히던 문제 수정
- 로그인한 플레이어의 닉네임이 캐릭터 머리 위에 네임택으로 표시되도록 추가
- 플레이어 네임택 높이를 작업 감독관 네임택과 더 비슷한 위치로 조정
- 메타마스크 없이도 닉네임만 설정해 바로 체험할 수 있는 `게스트로 시작` 모드 추가
- 메타마스크/게스트 시작 시 개발자 프리셋 장비를 제거하고 일반 신규 플레이어 상태로 시작하도록 분리

## 2026-04-04

### UI
- 우측 상단에 `마지막 패치` 시각 표시 추가
- `마지막 패치` 표시는 개발자 모드에서만 보이도록 변경
- 패치 시각 UI 초기화 순서 오류 수정
- `마지막 패치` 시각 수동 갱신

### 개발자 모드
- 개발자 모드 ON

### 맵 / 이동
- `광산맵`과 `폐광맵`을 연결하는 도보형 연결통로 구조를 유지하도록 조정
- 맵 도착 시 상단 배너에 `광산`, `폐광` 표시
- 거리 유지형 3인칭 카메라로 변경
- 이동 중에도 카메라 회전 각도와 거리 유지되도록 추적 방식 수정
- 폐광맵에 실내 느낌을 위한 외벽 추가
- 폐광맵 외벽 높이 40% 증가
- 폐광맵에서는 안개가 적용되지 않도록 변경
- 연결통로에서 폐광/광산 안개 전환이 부드럽게 이어지도록 수정
- 폐광맵은 안개 전환 대신 어둠 오버레이로 어둡게 보이도록 변경
- 전역 어둠 오버레이 제거, 폐광 쪽 조명만 어둡게 변경
- 전역 조명은 유지하고 폐광맵 재질만 더 어둡게 보이도록 변경
- 폐광맵 전용 짧은 시야 제한 추가
- 개발자 모드에서 `T` 키로 임시 횃불 시야 토글 가능
- 폐광 시야 제한 안개 색을 어둡게 조정해 하얗게 번지는 현상 수정
- `T` 횃불 토글 입력 분기 위치 오류 수정
- 횃불 활성화 시 폐광 시야 범위를 크게 확장하도록 조정

## 2026-05-02

### 인벤토리 구조
- 일반 아이템 슬롯 구조를 `{ kind: "item", itemId, count }` 형태의 객체형으로 1차 전환
- 장착 슬롯 내부 저장 구조를 객체형으로 바꾸되, 게임 로직은 기존처럼 `pickaxe`, `safetyHelmet` 문자열 기준으로 읽히도록 호환 레이어 추가
- 인벤토리 UI, 장착 UI, 튜토리얼 체크, 세이브/로드가 새 구조와 기존 저장 데이터를 모두 읽을 수 있도록 보강
- 실제 NFT 아이템이 아직 없어도 `kind: "nft"` 항목을 인벤토리/장착창/UI에서 이해하고 표시할 수 있는 2차 확장 뼈대 추가
- 개발자 모드에서만 인벤토리에 `mock NFT 장비`가 지급되도록 추가해 NFT 슬롯/장착/UI 동작을 직접 테스트할 수 있게 함
- mock NFT 안전모를 장착하면 일반 안전모 대신 금빛 전용 외형이 캐릭터와 장착 미리보기 모델에 표시되도록 보강
- mock NFT 안전모가 인벤토리와 장착 슬롯에서도 왕관 이모지 대신 실제 금빛 헬멧 프리뷰로 보이도록 정리

### 개발자 모드
- `개발자모드`와 `개발자 프리셋`을 같은 의미로 사용
- 개발자 모드 ON/OFF 시 시작 상태 전체가 함께 바뀌도록 유지
- 현재 상태: 개발자 모드 OFF

### 광산맵
- 외곽 시각 차단 구조물(`낮은 흙 절벽 + 큰 암석`) 복구
- 북서쪽 틈 메우기 복구
- 외곽 절벽선 충돌 복구
- 외곽 큰 암석 충돌 복구
- 북쪽 연결통로 쪽 완충 바닥 복구

### 폐광 입구 / 열쇠
- 개발자 모드 OFF 상태에서는 폐광 입구에 실제 울타리가 보이도록 수정
- `폐광 열쇠`를 사용해야만 울타리가 제거되도록 수정
- 울타리 중앙뿐 아니라 폭 전체 어느 위치에 닿아도 잠금 안내 문구가 뜨도록 수정

### 연결통로
- 연결통로 폭 확장 상태 유지
- 양옆에 시각 차단 구조물 추가
- 양옆 차단 구조물에 충돌 추가
- 차단 구조물 길이를 연결통로 길이와 동일하게 정리

### NPC
- 작업 감독관 NPC에 전용 충돌체 추가
- 작업 감독관 위치를 작업대 중심 북쪽으로 재배치

### 오브젝트 충돌
- 작업대 오른편 3개의 표지판에 전용 충돌체 추가

### 채굴 / 힌트
- 돌 근처에 가기만 해도 `곡괭이 필요` 문구가 뜨던 자동 안내 제거
- 이제 돌 근처에서는 `Space : 채굴`만 표시
- 곡괭이 필요 / 장착 필요 문구는 실제 채굴 시도 시에만 출력

### 참고
- 안개는 현재 꺼져 있음
- 나중에 다시 켤 경우 이전 합의값:
  - 색상 `0xc4d6e6`
  - `near: 13`
  - `far: 50`

### 공기 오염 / 정화 프로토타입
- 폐광 오염 입자 필드가 시간 경과에 따라 구역 밖으로 흘러가던 문제를 수정해서, 오염 입자가 폐광 내부에 계속 머무르도록 고정
- 폐광 오염 입자 높이 분포를 눈높이 중심에서 바닥부터 천장까지 더 고르게 퍼지도록 조정
- 폐광 오염 입자에 깊이 판정을 다시 적용해서, 벽 안쪽 입자가 폐광 밖에서 벽을 뚫고 보이지 않도록 수정
- 폐광 오염 입자는 플레이어 위치와 무관하게 폐광 구역 기준으로 항상 보이도록 바꾸고, 저산소 화면 반응만 현재 플레이어 위치 기준으로 유지
- 광산과 폐광 사이 연결통로를 공기 시스템상 호흡 가능한 완충 구역으로 분리해서, 통로 안에서는 산소가 줄지 않고 서서히 회복되도록 수정
- 저산소 화면 반응이 거의 체감되지 않던 문제를 보강해서, 산소 50% 이하부터 더 뚜렷해지고 0% 근처에서는 위기감이 확실히 느껴지도록 오버레이 강도를 상향 조정
- 저산소 화면 반응을 단순 색막에서 `가장자리 뿌연 흐림` 중심으로 재설계하고, 50% 이하는 약하게, 20% 이하는 급격히 심해져 거의 안 보이는 수준까지 체감되도록 곡선을 조정
- 저산소 화면 반응이 여전히 약하다는 피드백에 맞춰 가장자리 흐림 강도와 오버레이 세기를 크게 상향해서, 이전보다 약 3배 가까운 체감으로 위기감이 들도록 조정

## 2026-05-08

### 개척지 / 입력 / 인벤토리 비주얼
- 개척지 오염 맵에도 폐광처럼 전용 오염 먼지 입자가 보이도록 오염 입자 필드를 맵별로 분리 확장
- Shift로 달리는 중에도 좌클릭 드래그로 카메라를 회전할 수 있도록 입력 처리 보강
- 인벤토리에서 `나무조각`과 `목재`가 서로 다르게 보이도록 아이콘을 분리

### 자원 밸런스 / 개발자 편의
- 광산맵 바위 크기 3단계에 따라 `돌가루` 보상이 각각 다르게 나오도록 조정
- 소비/기타 아이템 스택 최대치를 200으로 통일하고, 초과 수량은 다음 인벤토리 칸으로 자동 분배되도록 인벤토리 추가/소비 로직 보강
- 신규 시작 상태의 기본 곡괭이 레벨을 1로 고정
- 개발자 모드에서는 현재 존재하는 모든 재료 아이템을 자동으로 200개씩 지급하도록 확장

### 자원 / 제작 확장
- 광산맵 나무에 `Space : 나무 채집` 상호작용을 추가하고, 채집 시 `나무조각`을 획득하도록 확장
- 나무는 사라지지 않고 10초 동안 비활성 상태가 되었다가 다시 채집 가능하도록 재생성 루프 추가
- 재련소 레시피에 `나무조각 2개 -> 목재 1개`를 추가하고, 목재를 향후 건축용 재료로 사용할 수 있게 신규 아이템 정의 추가
- 재련소 설명 문구를 레시피별로 분기해서 정화 가루와 목재가 각자 다른 용도로 보이도록 정리

### 폐광 자원
- 폐광맵에 광산 바위와 구분되는 더 어둡고 각진 전용 채굴 돌을 랜덤 배치
- 폐광 전용 돌은 장착 중인 곡괭이 Lv.4 이상일 때만 채굴 가능하게 하고, 성공 시 `석재`를 획득하도록 추가
- 광산맵 기존 바위 채굴 보상은 계속 `돌가루`로 유지

### 새 맵 / 개척지
- 폐광 북쪽에 새 연결통로를 추가하고, 그 너머에 신규 맵 `개척지`를 배치
- 개척지는 폐광 usable area 절반 정도 크기의 평평한 땅으로 생성
- 폐광 ↔ 개척지 연결통로는 회복 구역으로 판정되도록 추가
- 개척지는 처음에는 오염 맵으로 시작하고, 입구에 폐광과 동일한 공기 정화탑을 배치
- 세이브/로드에 `개척지` 맵 ID와 정화율 저장값을 함께 보존하도록 확장
- 개척지 내부를 `P1~P6` 6개 필지로 시각 분할하고, 통로 폭을 남겨 건축 예정 구역 구조를 한눈에 볼 수 있게 정리
- 각 필지 외곽에 밝은 색 테두리를 추가하고, 통로 쪽에 `P1~P6` 팻말을 배치
- 개척지 공기 정화탑을 P6 구역 내부가 아니라 연결통로가 끝나고 개척지가 시작되는 입구 쪽으로 이동
- 개척지 P6 건축물을 `0/25/50/75/100%` 5단계로 확장하고, 건물 높이를 캐릭터 2배 수준으로 키우며 정면 상단 부착형 간판과 단계별 충돌을 추가
- P6 위에서 `B : 건축 진행` / 완공 후 `B : 간판 수정` 힌트를 표시하고, 건축이 25% 이상 진행되면 P6 필지 번호 팻말이 자동으로 숨겨지도록 조정
- P6 건축물 충돌이 실제 월드 위치에 맞게 작동하도록 건축 그룹 생성 순서를 조정하고, 정면 상단 간판 보 길이도 충돌에 포함되게 수정
- P6 간판을 2.5배 넓히고 폰트를 3배 수준으로 키우며 최대 입력 글자 수를 8자로 제한하고, 개척지 정화장치를 입구 오른쪽 끝으로 이동
- 개척지 정화장치를 P6 경계에서 더 멀어지도록 남쪽 벽 쪽으로 추가 이동해 보행 동선과 필지 침범을 줄임
- `Mansion ONE 101호` 권한 아이템을 추가하고, 서쪽 주거동 입구에서 권한 보유자만 포탈형 개인 거주 공간으로 입장할 수 있게 연결
- 개인 거주 공간 내부에 출구 문, 작은 창고, 침대를 배치하고, 출구 문 앞 `E` 상호작용으로 다시 `Mansion ONE` 입구 앞으로 복귀하도록 추가
- 개인 거주 공간의 작은 창고에 `Space : 창고 열기` 상호작용과 `인벤토리 / 개인 창고` 드래그 앤 드롭 UI를 추가하고, 창고 내용이 세이브/로드에 유지되도록 확장
# 2026-05-11 16:47:25 KST - 개척지 부스 E키 상호작용/관리·보기 UI 분리 1차
- `B`는 필지 건축/건축 관리 전용으로 유지하고, 완공 후 상점/전시 부스는 부스 앞 `E`키 상호작용으로 분리했다.
- 상점 부스는 운영자/상점 사용자에게 `판매 관리`, 일반 방문자에게 `판매 보기`가 뜨도록 했고, 전시 부스도 `전시 관리`/`전시 보기` 힌트로 분기했다.
- 완공 건축물 내부 부스마다 월드 상호작용 마커를 따로 생성하고, 기존 필지 운영창에서는 판매 등록/상점 사용자 관리 기능을 걷어내 건축 화면과 운영 화면의 역할을 분리했다.
# 2026-05-11 16:52:42 KST - 개척지 부스 관리 UI 클릭 버그 수정
- 부스 관리/보기 창 본체에 `pointerEvents`가 빠져 클릭이 뒤 오버레이로 떨어지던 문제를 수정했다.
- 이제 상점 관리 UI 안의 버튼과 입력칸이 정상적으로 클릭/입력되고, 클릭 시 창이 바로 닫히지 않는다.
# 2026-05-11 19:22:02 KST - 개발자1/개발자2 역할 분리와 Mansion ONE 101/102호 분기
- 개발자 모드의 역할을 더 분명히 나누기 위해 `Mansion ONE 102호` 권한 아이템을 추가하고, 개발자1은 `Mansion ONE 101호`, 개발자2는 `Mansion ONE 102호`만 보유하도록 프리셋과 역할 보정 로직을 확장했다.
- 개발자2 프로필에는 개척지 `P1~P6` 개발권 아이템이 남지 않도록 자동 정리하고, 개발자1만 필지 운영자 역할을 유지하게 조정했다.
- 주거동 입구 상호작용을 권한 기반으로 분기해 각 개발자 프로필이 자신에게 배정된 방으로만 입장하도록 바꾸고, `102호`도 `101호`와 같은 구조의 침대/창고/출구 문이 있는 포탈형 개인 거주 공간으로 추가했다.
# 2026-05-11 19:36:29 KST - 개척 코인 별도 잔액 구조 1차
- 상점 결제용 전용 화폐를 인벤토리 슬롯 아이템이 아닌 별도 잔액 값 `개척 코인`으로 추가하고, 이후 구매/판매 시스템이 바로 연결할 수 있는 기본 헬퍼 함수들을 정리했다.
- 개발자 모드는 테스트를 위해 시작 시 `500원`을 받도록 하고, 게스트/메타마스크/일반 새 시작 상태는 `0원`으로 시작하도록 초기값을 분리했다.
- 개인 세이브에 화폐 잔액을 함께 저장/복원하도록 확장하고, Equipment 하단 프로필 카드에 현재 `개척 코인` 잔액을 표시하도록 최소 UI를 추가했다.
# 2026-05-11 19:41:12 KST - 개발자 저장본 개척 코인 마이그레이션 보정
- 기존 개발자 프로필 저장본에는 `economy.credits`가 없어서 `개척 코인`이 0원으로만 로드되던 문제를 보정했다.
- 개발자 세이브를 불러올 때 화폐 값이 아예 없는 예전 저장본이면 자동으로 `500원`을 채워 넣도록 마이그레이션 처리했다.
# 2026-05-11 19:43:29 KST - 개발자 0원 저장본 1회 개척 코인 보정
- 이미 `0원`으로 저장돼 있던 기존 개발자 프로필도 한 번만 `500원`으로 올려주는 프로필별 마이그레이션 키를 추가했다.
- 이제 개발자1/개발자2 모두 예전 0원 저장본이라도 첫 재입장 시 `개척 코인 500원`을 받고, 그 이후에는 실제 테스트 중 사용한 잔액을 그대로 유지한다.
# 2026-05-11 19:49:25 KST - 상점 구매자용 판매 보기와 개척 코인 결제 1차
- 완공된 필지의 상점 부스에서 일반 방문자/구매자가 `E : 판매 보기`로 판매 목록을 열고, 등록된 상품의 재고/가격을 확인할 수 있는 구매자용 UI를 추가했다.
- 판매 보기 UI에서 구매 수량을 입력한 뒤 `구매`를 누르면 구매자 `개척 코인` 차감, 구매자 인벤토리 지급, 판매 슬롯 재고 감소, 가판대 수량 표식 갱신이 한 흐름으로 처리되도록 연결했다.
- 판매자 정산은 개발자 테스트 환경 기준으로 `개발자1/개발자2` 프로필의 개인 세이브 잔액까지 반영되게 해서, 개발자1 판매자 / 개발자2 구매자 시나리오를 실제로 검증할 수 있게 했다.
# 2026-05-11 19:53:18 KST - 상점 구매 수량 입력 한 자리수 버그 수정
- 구매 수량 입력창이 비워지는 즉시 다시 `1`로 덮어써져 한 자리 수를 새로 입력하기 어렵던 문제를 수정했다.
- 이제 입력 중에는 값을 강제로 덮어쓰지 않고, 포커스 시 자동 선택되며, blur/구매 시점에만 정상 수량으로 보정되도록 바꿨다.
# 2026-05-11 20:08:25 KST - 개발자모드 테스트 초기화 버튼과 역할별 프리셋 복원
- 개발자모드 상단 전환 UI에 `테스트 초기화` 버튼을 추가하고, 확인 후 개척지 공용 월드 상태와 개발자1/개발자2 개인 상태를 함께 초기값으로 되돌리는 흐름을 만들었다.
- 초기화 후 개발자1은 `곡괭이 Lv.5`, 안전모 장착, 현재 존재하는 모든 `기타/소비` 아이템 200개씩, `P1~P6 개발권`, `Mansion ONE 101호`, `개척 코인 500원`을 갖는 운영자 프리셋으로 복원되게 했다.
- 초기화 후 개발자2는 필지 개발권 없이 `Mansion ONE 102호`와 `개척 코인 500원`만 가진 방문자/구매자 테스트용 프리셋으로 복원되게 정리했다.
# 2026-05-11 20:14:05 KST - 개발자1 대량 지급 아이템 필터 수정
- 개발자1 초기화 프리셋에서 `기타/소비` 전체를 넓게 잡으면서 `폐광 열쇠`처럼 `stackMax: 1`인 비소모성 아이템까지 200개씩 지급되던 문제를 수정했다.
- 이제 대량 지급 대상은 `권한 아이템이 아니고`, `카테고리가 소비/기타이며`, `실제로 2개 이상 스택 가능한 아이템`만 포함되도록 필터를 좁혔다.
# 2026-05-11 20:18:38 KST - 개발자1 초기화 지급 대상 아이템 ID 판정 수정
- 개발자1 테스트 초기화 프리셋에서 `200개 지급 대상`을 고를 때 아이템 정의 안의 없는 필드(`def.itemId`)를 보고 있던 버그를 수정했다.
- 이제 실제 아이템 ID 기준으로 `소비/기타 + 스택 가능 + 권한 아이템 아님 + 폐광 열쇠 제외` 조건을 정확히 판정해서, `정화 가루`, `돌가루`, `목재`, `석재` 같은 재료만 200개씩 지급되게 했다.
# 2026-05-11 23:36:23 KST - 개발자1 초기화 인벤토리 구성 강제 보정
- 개발자1 테스트 초기화 프리셋의 `200개 재료 지급` 대상을 명시 목록(`신선한 공기 캔`, `나무조각`, `목재`, `정화 가루`, `돌가루`, `석재`)으로 고정해서, 열쇠류나 권한 아이템이 다시 대량 지급 경로로 섞이지 않게 했다.
- `폐광 열쇠`, `개척지 P1~P6 개발권`, `Mansion ONE 101호/102호`처럼 `1개만 있어야 하는 아이템`은 프리셋 적용 마지막에 중복을 모두 지우고 정확히 1개만 남기도록 강제 보정 로직을 추가했다.
# 2026-05-12 15:11:19 KST - Mansion ONE 개인 호실 로컬 인스턴스 전환
- Mansion ONE 101호/102호를 주거 구역 생성 시점에 월드에 항상 올려두지 않고, 입장할 때만 생성하고 퇴장할 때 제거하는 로컬 인스턴스 구조로 바꿨다.
- 방 안의 벽/침대/창고/출구 문 메쉬와 충돌체, 상호작용 마커를 호실 인스턴스에 묶어서 생성/제거되게 했고, 개인 창고 데이터는 기존 개인 세이브를 그대로 재사용해서 유지되게 했다.
- 프로필 전환이나 테스트 초기화 때도 남아 있는 호실 인스턴스를 정리하도록 안전 보정을 추가했다.
- 2026-05-12 15:45:39 KST
  - 개인 호실(101호/102호) 안에서 새로고침 후 재접속하면 호실 인스턴스를 다시 생성한 뒤 저장된 위치로 복구되도록 수정
- 2026-05-12 15:55:39 KST
  - 권한 부족 안내가 하단 힌트에 상시 고정되지 않도록 수정하고, 개척지 필지 개발권/거주권 부족 문구는 해당 상호작용 키를 눌렀을 때만 잠깐 표시되도록 정리
- 2026-05-12 16:04:51 KST
  - 광산 나무 채집 시 기존 채굴 스윙 모션을 재사용하고, 나무 전체가 좌우로 짧게 흔들리며 감쇠해 멈추는 피드백 연출 추가
- 2026-05-12 17:29:51 KST
  - `기본신발` 장비 아이템 추가 및 시작 스톨에 배치
  - 메타마스크/게스트 모드 이동 속도를 신발 착용 여부와 연결: 미착용 걷기 3.0, 기본신발 착용 시 Shift 달리기 4.5
  - 개발자 모드는 신발 여부와 무관하게 이동 속도 7.0 고정
  - 기본신발 착용 시 캐릭터 발 부분에 연갈색 신발 비주얼 표시
- 2026-05-12 17:38:43 KST
  - 기본신발 착용 후 Shift 달리기 판정을 개발자 세션 기준으로 수정
  - 기본신발 비주얼을 플레이어 루트가 아니라 좌우 다리에 각각 부착해 보행 모션을 따라가도록 보정

- 2026-05-12 17:47:48 KST
  - 기본신발 착용 시 달리기 체감이 더 분명하게 보이도록 보행 모션 위상을 가속하고, 신발 위치를 발끝에 더 가깝게 보정
- 2026-05-13 16:50:13 KST
  - 이동속도 조정을 쉽게 하도록 개발자모드 / 메타마스크·게스트 걷기 / 기본신발 달리기 튜닝값 주석과 전용 상수 추가
- 2026-05-13 17:03:28 KST
  - 기본신발 비주얼을 다리 직접 부착에서 발끝 anchor 방식으로 변경하고, 신발 한 짝 모델 중심을 앞으로 보정해 발과의 간격을 줄임
- 2026-05-13 17:16:10 KST
  - 인벤토리 창 가로 폭을 넓히고 장비창 위치를 같이 조정해 5칸 슬롯이 수평 스크롤 없이 보이도록 수정
  - 인벤토리 슬롯 스크롤 영역에 가로 오버플로우를 숨겨 불필요한 하단 스크롤바가 나타나지 않도록 정리
- 2026-05-13 19:22:11 KST
  - 판매 물품이 모두 팔려 품절된 뒤에도 실제 판매자에게 `정산금 회수` UI가 계속 남도록 상점 판매자 도구 표시 조건을 보강
  - 활성 판매가 없더라도 정산 대기금이 남아 있으면 판매자가 부스에서 정산금을 회수할 수 있게 수정
- 2026-05-13 19:13:26 KST
  - 필지 운영자가 실제 판매자가 아닐 때도 상점 부스 관리 창 안에서 현재 등록 상품을 확인하고 방문자처럼 구매할 수 있도록 상점 보기/구매 패널을 함께 표시
  - 상점 구매용 카드 렌더 로직을 공통화해 관리 모드와 보기 모드에서 같은 상품 상태/구매 흐름을 재사용
- 2026-05-13 19:07:39 KST
  - 상점/전시 부스 UI를 운영자 / 슬롯 사용자 / 방문자 역할 기준으로 더 분리해, 운영자에게만 사용자 지정 UI가 보이도록 정리
  - 상점 판매자와 전시 사용자는 자기 관리 UI만 보고, 운영자이지만 슬롯 사용자가 아닌 경우에는 지정 상태와 운영 요약만 확인하도록 조정
- 2026-05-13 19:04:46 KST
  - 전시 관리 UI를 좌우 분할 레이아웃으로 개편해 왼쪽에는 상태/사용자 지정/관리 버튼을 고정하고, 오른쪽에는 전시 가능한 인벤토리 목록만 세로 스크롤되도록 정리
  - 전시 관리 모달 폭과 최대 높이를 늘려 상단/하단이 잘리는 문제를 줄이고, 기능 로직은 유지한 채 레이아웃만 개선
- 2026-05-13 18:25:51 KST
  - 상점 운영 권한을 운영자/판매자로 분리하고, 판매자가 등록한 상품의 가격 수정·회수·정산금 회수만 가능하도록 보강
  - 상점 판매 대금을 즉시 지급하지 않고 부스 정산 대기금으로 누적한 뒤, 판매자가 직접 회수하는 흐름 추가
  - 전시 슬롯 A/B에 사용자 지정 기능을 추가하고, 전시 관리/보기 UI를 운영자·전시 사용자·방문자 기준으로 분기
- 2026-05-15 14:52:28 KST
  - `frontier.js` 1차 분리: 개척지 필지 정의, 건축 단계, 운영 데이터 기본 구조, 권한/슬롯 판정, 상태 요약/정규화 같은 순수 로직을 `src/systems/frontier.js`로 이동
  - `main.js`는 개척지 월드 반영과 UI/상호작용은 유지하고, frontier 순수 함수들은 모듈 호출 래퍼로 정리
- 2026-05-15 14:58:50 KST
  - `frontier.js` 2차 분리: 개척지 build state 접근, 상점/전시 slot state 조회, 판매자/전시 사용자 판정, 전시 가능 아이템 판정, 구매 가능 여부 계산 같은 상태 조회/판단 헬퍼를 `src/systems/frontier.js`로 이동
  - `main.js`는 `showUI`, 저장 동기화, 월드 재빌드 같은 부수효과를 유지하고, frontier 상태 계산은 모듈 호출 래퍼 중심으로 정리
- 2026-05-15 15:04:17 KST
  - `frontier.js` 3차 분리: 전시 등록/해제, 전시 사용자 지정/해제, 상점 정산 대기금 회수에 필요한 순수 state 변경/검증 헬퍼를 `src/systems/frontier.js`로 이동
  - `main.js`는 메시지 표시, 저장 동기화, 월드 재빌드, UI 갱신 같은 부수효과만 유지하고 frontier 상태 변경은 모듈 호출 래퍼 중심으로 정리
- 2026-05-15 15:20:03 KST
  - `frontierUi.js` 분리: 개척지 건축 창, 상점 부스 UI, 전시 부스 UI의 DOM 생성/역할별 섹션 렌더링을 `src/ui/frontierUi.js`로 이동
  - `main.js`는 개척지 상태 계산, 버튼 액션 콜백 연결, 저장 동기화/월드 재빌드 같은 부수효과만 유지하고 UI 조립은 모듈 호출로 정리
- 2026-05-15 15:27:40 KST
  - `hud.js` 분리: 공기 HUD, 하단 메시지 UI, 나침반, 맵 도착 배너, 패치 시각/세이브 상태 배지의 DOM 생성과 레이아웃을 `src/ui/hud.js`로 이동
  - `main.js`는 HUD 표시 타이밍, 공기 수치 계산, 드래그 위치 저장, 메시지 제어 같은 부수효과만 유지하고 HUD 조립은 모듈 호출로 정리
- 2026-05-15 17:04:53 KST
  - `movement.js` 분리: 키 입력 해석, 걷기/달리기 속도 계산, 신발 착용 기반 Shift 달리기 판정, 보행 애니메이션 배속 계산을 `src/core/movement.js`로 이동
  - `main.js`는 플레이어 위치 반영, 충돌 판정, Three.js 회전/모션 적용 같은 부수효과만 유지하고 이동 계산은 모듈 호출로 정리
- 2026-05-15 17:10:29 KST
  - `collisions.js` 분리: 플레이어 충돌 박스 생성, collider Box3 교차 판정, 생성 직후 겹침 검사, 맵 경계 bounds 계산/판정을 `src/core/collisions.js`로 이동
  - `main.js`는 메시지 표시, 카메라 스냅, 실제 위치 보정 같은 부수효과만 유지하고 충돌 계산은 모듈 호출로 정리
- 2026-05-15 17:18:08 KST
  - `player.js` 1차 분리: 플레이어 본체, 머리, 몸통, 팔/다리 피벗 같은 기본 리그 생성 구조와 파츠 조회 헬퍼를 `src/core/player.js`로 이동
  - `main.js`는 장비 부착, 애니메이션, 이동/충돌/월드 반영, 장비창 미리보기 갱신 같은 부수효과만 유지하고 기본 리그 생성은 모듈 호출로 정리
- 2026-05-15 17:23:19 KST
  - `player.js` 1차 분리 보정: 로그인 직후 플레이어 초기화에서 쓰는 `leftArm/rightArm/leftLeg/rightLeg` 파츠 조회가 빠져 생기던 런타임 오류를 수정
  - `getPlayerRigParts()` 반환값과 `main.js` 구조 분해를 보강해 장비 부착 초기화가 다시 정상 동작하도록 정리
- 2026-05-15 17:30:40 KST
  - `player.js` 2차 분리: 곡괭이/안전모/NFT 헬멧/기본신발의 플레이어 장비 비주얼 부착 구조와 표시 토글을 `src/core/player.js`로 이동
  - `main.js`는 장비 아이템 상태를 읽어 어떤 비주얼을 보일지만 넘기고, 실제 플레이어 파츠 부착은 모듈 호출로 정리
- 2026-05-15 17:37:46 KST
  - `player.js` 3차 분리: 수면 포즈, 걷기/달리기 포즈, 채굴 스윙, 줍기 리치, 장비창 미리보기 포즈 복사 같은 플레이어 모션 적용 헬퍼를 `src/core/player.js`로 이동
  - `main.js`는 이동/채굴 상태와 시간값만 계산하고, 실제 플레이어 포즈 적용은 모듈 호출로 정리
- 2026-05-15 17:57:41 KST
  - `scene.js` 1차 분리: 메인 scene, fog/background 분위기, 기본 조명, camera, renderer, gridHelper 초기화 코드를 `src/core/scene.js`로 이동
  - `main.js`는 게임 루프와 오브젝트/시스템 연결을 유지하고, 기본 씬 구성은 모듈 호출로 정리
- 2026-05-15 18:03:46 KST
  - `movement.js` 2차 분리: 입력 기반 move vector 생성, 이동 여부 판정, latestMoveDir 갱신, delta movement 계산, yaw 계산을 `src/core/movement.js`로 이동
  - `main.js`는 충돌 차단과 실제 위치 반영을 유지하고, 이동 적용 직전의 벡터/방향 계산은 모듈 호출로 정리
- 2026-05-15 18:07:27 KST
  - `collisions.js` 2차 분리: X축/Z축 이동 시도 후 충돌 검사와 위치 되돌리기, 맵 경계/시작링 차단을 함께 처리하는 이동 충돌 적용 헬퍼를 `src/core/collisions.js`로 이동
  - `main.js`는 이동 벡터와 실제 후속 보정 호출을 유지하고, 이동 후 막힘 판정은 모듈 호출로 정리
- 2026-05-15 18:13:08 KST
  - `movement.js` 3차 분리: 카메라 기준 forward/right 이동축 계산, 회전 기반 facing direction 계산, 나침반용 방향 fallback 계산을 `src/core/movement.js`로 이동
  - `main.js`는 이동 상태와 충돌 적용을 유지하고, 이동 기준축/방향 보조 계산은 모듈 호출로 정리
