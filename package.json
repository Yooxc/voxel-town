{
  "name": "voxel-town",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:wasteland": "node --test tests/frontier.wasteland.test.js",
    "server:dev": "npm --prefix server run dev",
    "server:start": "npm --prefix server run start"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "three": "^0.182.0"
  }
}
